{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "installCommand": "npm install && cd ../node && npm install",
  "functions": {
    "src/app/api/generate/route.ts": {
      "memory": 1024,
      "maxDuration": 60
    }
  }
}
